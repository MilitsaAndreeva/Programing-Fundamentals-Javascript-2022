function requiredReading(pagesQuantity, pagesPerHour, daysQuantity){

    let hoursPerDay = (pagesQuantity/pagesPerHour)/daysQuantity;
    console.log(hoursPerDay);
}
requiredReading (432,

    15 ,
    
    4)